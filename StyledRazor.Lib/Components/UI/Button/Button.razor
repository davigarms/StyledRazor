@inherits UIComponents

<Layout _="DefaultButton"
        style=@($@"
          --margin: {Margin ?? ButtonMargin};
          --padding: {Padding ?? ButtonPadding};")>
  @ChildContent
</Layout>

@code {
  [Parameter]
  public RenderFragment ChildContent { get; set; }

  [Parameter]
  public string Margin { get; set; }

  [Parameter]
  public string Padding { get; set; }
}